<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Budgetr" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Transparent theme for QuickAddActivity so the bottom sheet overlays the home screen -->
    <style name="Theme.Budgetr.QuickAdd" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
    </style>
</resources>
